<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary">

	<LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:layout_above="@id/flLinkCheckerMenuContainer">

		<!-- Title -->
		<TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Link Checker"
            android:textColor="@color/textPrimary"
            android:textSize="34sp"
            android:textStyle="bold"
            android:layout_marginTop="40dp" />

		<TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Paste a link below to check if it's safe."
            android:textColor="@color/textPrimary"
            android:textSize="14sp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="24dp" />

		<!-- URL Input -->
		<EditText
            android:id="@+id/etLink"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="https://example.com"
            android:textColor="@color/textPrimary"
            android:textColorHint="@color/hintText"
            android:background="@android:drawable/edit_text"
            android:inputType="textUri"
            android:padding="10dp"
            android:singleLine="true" />

		<!-- Check Button -->
		<Button
            android:id="@+id/btnCheckLink"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="16dp"
            android:text="Check Link"
            android:textSize="16sp"
            android:textColor="@color/buttonText"
            android:background="@color/colorPrimaryDark"
            android:textAllCaps="false" />

		<!-- Loading Spinner -->
		<ProgressBar
            android:id="@+id/pbLoading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="16dp"
            android:visibility="gone" />

		<!-- Result TextView -->
		<TextView
            android:id="@+id/tvResult"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:textSize="16sp"
            android:textStyle="bold"
            android:gravity="center"
            android:visibility="gone" />

	</LinearLayout>

	<!-- Menu fragment container -->
	<FrameLayout
        android:id="@+id/flLinkCheckerMenuContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true" />

</RelativeLayout>
